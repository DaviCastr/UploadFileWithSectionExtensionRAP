projection;
strict ( 2 );
use draft;

define behavior for YC_RAP_INVOICE_EXT_DFLC //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}